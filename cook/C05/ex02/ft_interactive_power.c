int		ft_interactive_power(int nb, int power)
{
	int result;

	result = 1;
	if (power < 0)
		return (0);
	if (power == 1)
		return (1);
	while(i < power)
	{
		result = result * nb
	}
	i++;
	return (result);

}
